# 上游同步摘要

- 日期：2026-09-09
- 模式：只报告，未改写本地分支
- 上次同步：v0.10.7（2026-09-09T00:43:26+08:00），来源 `upstream/master`
- 本次端点：`upstream/master` = `41f55c7`（`Cargo.toml` / `package.json` 已写 0.10.8，GitHub 尚未发 Release / 尚未打 `v0.10.8` 标签）
- 本分叉：已在 origin 发布自己的 `v0.10.8`（标签指向 `dde98b1`，与上游不是同一提交）

## 范围

- 源：`upstream/master`
- 方向：`master` ← 8 个上游独有提交，112 个文件
- 分叉独有提交（rebase 会重放）：23
- 工作区未提交（本次未处理）：`INTERNAL_DEVELOPMENT.md`、`public/composer-usage.js`、`tests/composer-usage.test.mjs`、`tests/helpers/fake-element.mjs`

## 标签说明

上游 GitHub 最新 Release 仍是 v0.10.7。`git ls-remote --tags upstream v0.10.8*` 为空。本地 / origin 的 `v0.10.8` 是本分叉标签，不要当成上游标签。

## 破坏性变更（2）

先读这些。

- `a4c71a0` Simplify application architecture and remove obsolete code  
  60 个文件。控制台从 Mantine 换成 Ant Design（去掉 `@mantine/core` / `@mantine/hooks` 9.5.2，加入 `antd` ^6.6.2）。本分叉仍用 Mantine。会改写 `ModelSection` / 选择器 / 样式，并改动本分叉已删的 `RequestLogDialog.tsx`。
- `432bbb8` Update application features and supporting code  
  75 个文件。加强内置 `local_router`（含 `native_history.rs`）、重做请求日志与诊断清理 API（`clear_diagnostic_storage` 改为必须带 `target`）。官方线路去掉上下文预算 / 1M 入口。与 ADR `docs/adr/0001-remove-built-in-router.md` 冲突。

## 功能（4）

- `122d4e0` Improve Fast options and compact request log overview  
  所有线路/模型展示 Fast；请求日志改紧凑概览。Fast 注入与本分叉方向相近，日志 UI 本分叉已删。
- `c804b39` Preserve sanitized upstream error details in request logs  
  请求日志展示脱敏后的上游错误正文。主要改 `local_router` 与已删的 `RequestLogDialog.tsx`。
- `b6c1a52` Preserve route metadata while sending raw turn model IDs  
  线路元数据 + 原始 turn 模型 ID。依赖内置路由，与本分叉注入定制重叠。
- `432bbb8` 另含：Windows 覆盖层修复（`overlay_recovery` / `PetWindowAgent.cs`）、Trace/Crashpad 分开清理通知、本地路由短暂断线恢复。

## 修复（2）

- `c588126` fix: align subagent models with the active runtime catalog  
  子代理角色按当前运行目录生成；走 `local_router_enabled` 分支。本分叉已固定 native 路径。
- `c2cf0d5` Merge PR [#34](https://github.com/SuperGness/codey/pull/34) and preserve fallback subagent routes during hot reload  
  目录回退后避免子代理 `Unknown model`；热重载失败时回退线路。父提交含 `432bbb8` 与 `c588126`。

## 依赖升级（1）

重装几乎不可避免。

- `@mantine/core` 9.5.2 → 删除
- `@mantine/hooks` 9.5.2 → 删除
- `antd`：无 → `^6.6.2`
- `@ant-design/cssinjs`：无 → `^2.1.2`
- 版本号：`0.10.7` → `0.10.8`（本分叉 `package.json` 已是 0.10.8）

静默合并风险：`package.json` 的依赖块只有上游改过，三路合并会直接收成 Ant Design，而源码冲突若选 keep-fork 仍是 Mantine。`pnpm-lock.yaml` 本分叉在该区间未改，也会整份收上游锁文件。

## 迁移 / 模式（1）

- 诊断清理改为按 `trace` / `crashpad` 分目标；删除联合诊断面板与 `TraceLogModule` 轮询。本分叉仍保留旧 Trace 模块。

## 重构 / 样式 / 文档 / 测试 / CI（合计计入上面的大提交）

`a4c71a0`、`432bbb8` 已覆盖。另有 `41f55c7` Bump version to 0.10.8（4 个文件，附带 `local_router/websocket.rs` 格式微调）。

## 冲突面（merge-tree 预演，未真正 rebase）

- 锁文件 / 生成物（会按 theirs + 重生）：`Cargo.lock` 显示可自动合并；`pnpm-lock.yaml` 预演未报冲突，会整份收上游
- 分叉私有（`docs/`、`CONTEXT.md`）：0 个冲突文件
- 真正重叠（需要决定）：33 个路径
  - 内容冲突约 21：`README.md`、`INTERNAL_DEVELOPMENT.md`、`launcher.rs`、`commands.rs`、`App.tsx`、`ModelSection.tsx`、`FeaturePolicyCard.tsx`、`PromptOptimizationCard.tsx`、`ModelCombobox.tsx`、`overlay.tsx` 及多份样式/测试
  - 修改/删除约 12：`backend/src/local_router/**`（本分叉已删、上游继续改，10 个文件）、`src/RequestLogDialog.tsx`（本分叉已删）、`tests/mantine-wrapper.test.mjs`（上游已删）

## 本分叉独有提交（23，rebase 会重放）

同步 v0.10.7 之后又增加 7 个，含本分叉自己的 `release: v0.10.8`。其余 16 个是上次接到 v0.10.7 时重放的二开。

- `dde98b1` Allow clearing the Windows read-only bit in Codex config tests.
- `5220bb6` Keep runtime status tests off the Codex version lookup.
- `28e31a0` Fix Windows catalog TOML fixtures and give runtime status more time.
- `5a78816` release: v0.10.8
- `940668e` Align replayed tests and keep the clippy release gate green.
- `66bee42` Restore WSL app-server rewrite and align tests with the replayed tree.
- `53332f9` Move conversation usage and account quota onto the Codex composer toolbar.
- …以及接到 v0.10.7 时保留的路由移除、只读线路控制台、模型目录、禁用自动更新等（从 `b1026ff` 到 `11441c5`）

## 建议

不要整段 rebase 到 `upstream/master`。

原因：上游 0.10.8 尚未打标签；两笔大提交会把已删除的 `local_router` 和 `RequestLogDialog` 带回来，并把 UI 库换成 Ant Design。这与 ADR 0001 和本分叉已发布的 v0.10.8 产品形态冲突。

若之后仍要取上游能力，按文件挑选，不要整提交合并：

1. `122d4e0` 里 Fast 注入（丢掉请求日志 UI）
2. 可选：`432bbb8` 里 Windows 覆盖层修复（`overlay_recovery`、NSIS、许可证），需确认不依赖路由
3. 拒绝：任何恢复 `backend/src/local_router/**` 或 `src/RequestLogDialog.tsx` 的路径
4. 跳过：`41f55c7` 版本 bump（本分叉已是 0.10.8）

## 上游提交清单（新 → 旧）

```
41f55c7 Bump version to 0.10.8
c804b39 Preserve sanitized upstream error details in request logs
122d4e0 Improve Fast options and compact request log overview
c2cf0d5 Merge PR #34 and preserve fallback subagent routes during hot reload
432bbb8 Update application features and supporting code
c588126 fix: align subagent models with the active runtime catalog
a4c71a0 Simplify application architecture and remove obsolete code
b6c1a52 Preserve route metadata while sending raw turn model IDs
```

核对：`git rev-list --count HEAD..upstream/master` = 8。列表完整（`git log` 末行无换行时 `wc -l` 会少计 1，已用 `rev-list` 交叉验证）。
